# Food-Vision---Image-Recognizer
Food Vision is a deep learning project that classifies food images into 101 categories using transfer learning with EfficientNetB0. The model is trained on the Food-101 dataset using TensorFlow, optimized with tf.data pipelines, and supports accurate real-world food image prediction.
